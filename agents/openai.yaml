interface:
  display_name: "Excel PubMed Fulltext Review"
  short_description: "Excel-to-PubMed PDF review pipeline with scansci and institutional fill"
  default_prompt: "Use $excel-pubmed-fulltext-review to download PubMed-linked papers from my Excel file, retry with scansci-pdf and 泉方/本地PubMed when needed, then summarize them into Excel."
policy:
  allow_implicit_invocation: true
